name: Bump NPM Package Version

on:
  workflow_dispatch:
    inputs:
      version_type:
        description: "Specify if the version type bump is 'major', 'minor', or 'patch'."
        required: true
        default: "patch"

jobs:
  update-npm-version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Update package version
        run: |
          if [ -z "${{ github.event.inputs.version_type }}" ]; then
            echo "Version type input is required!"
            exit 1
          fi

          if ! [[ "${{ github.event.inputs.version_type }}" =~ ^(major|minor|patch)$ ]]; then
            echo "Version type input must be 'major', 'minor', or 'patch'!"
            exit 1
          fi

          npm version $VERSION_TYPE
          git push origin HEAD:version-bump-${{ github.event.inputs.version_type }}
      
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Update NPM package version
          title: Update NPM package version
          body: Automatic update to NPM package version using GitHub Actions
          branch: new-version
          base: main

      - name: Wait for build to succeed
        uses: fountainhead/action-wait-for-check@v1.1.0
        id: wait-for-build
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          checkName: build
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Merge Pull Request
        uses: juliangruber/merge-pull-request-action@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          number: ${{ github.event.pull_request.number }}
          method: squash # merge rebase
          delete_branch: true
          token: ${{ secrets.GITHUB_TOKEN }}
          repo: juliangruber/octokit-action
          
